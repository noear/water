三种字段策略：

xxxxx：公开字段  //会记录值，会自动缓存  //注：调用this._$c时不自动缓存

_xxxx：私有字段  //不记录值，会自动缓存  //注：调试时会记录值，调用this._$c时不自动缓存

_$xxx：私密字段  //不记录值，不自动缓存




**私有字段，//主要用来加载大块数据 //为了控制记录的大小而设计

**私密字段，//主要用来加载模型（或引擎内部使用的一些字段）